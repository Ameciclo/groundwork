# Traefik Dashboard IngressRoute
# Exposes the Traefik dashboard via Traefik's own routing system
# This allows the dashboard to be accessible on the traefik entrypoint (port 8080)

apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: traefik-dashboard
  namespace: kube-system
spec:
  entryPoints:
    - traefik
  routes:
    # Redirect root to /dashboard/ (exact match, highest priority)
    - match: Path(`/`)
      kind: Rule
      priority: 1000
      services:
        - name: noop@internal
          kind: TraefikService
      middlewares:
        - name: redirect-root-to-dashboard
    # Redirect /dashboard to /dashboard/ (exact match, high priority)
    - match: Path(`/dashboard`)
      kind: Rule
      priority: 999
      services:
        - name: noop@internal
          kind: TraefikService
      middlewares:
        - name: redirect-dashboard-to-dashboard-slash
    # Dashboard UI (prefix match, lower priority)
    - match: PathPrefix(`/dashboard`)
      kind: Rule
      priority: 100
      services:
        - name: api@internal
          kind: TraefikService
    # API endpoints (prefix match, lower priority)
    - match: PathPrefix(`/api`)
      kind: Rule
      priority: 100
      services:
        - name: api@internal
          kind: TraefikService
---
# Middleware to redirect / to /dashboard/
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: redirect-root-to-dashboard
  namespace: kube-system
spec:
  redirectRegex:
    regex: ^https?://([^/]+)/?$
    replacement: https://${1}/dashboard/
    permanent: true
---
# Middleware to redirect /dashboard to /dashboard/
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: redirect-dashboard-to-dashboard-slash
  namespace: kube-system
spec:
  redirectRegex:
    regex: ^https?://([^/]+)/dashboard/?$
    replacement: https://${1}/dashboard/
    permanent: true
