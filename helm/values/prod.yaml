# Production Environment Values
# Override default chart values for production deployments

# Example: Tailscale operator configuration for production
# tailscale-operator:
#   replicaCount: 3
#   resources:
#     limits:
#       cpu: 500m
#       memory: 512Mi
#     requests:
#       cpu: 250m
#       memory: 256Mi
#   affinity:
#     podAntiAffinity:
#       preferredDuringSchedulingIgnoredDuringExecution:
#       - weight: 100
#         podAffinityTerm:
#           labelSelector:
#             matchExpressions:
#             - key: app.kubernetes.io/name
#               operator: In
#               values:
#               - tailscale-operator
#           topologyKey: kubernetes.io/hostname

