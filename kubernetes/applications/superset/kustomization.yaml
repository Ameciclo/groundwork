---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: superset

resources:
  - namespace.yaml
  - ingress.yaml

# Note: Main Superset deployment is managed by Helm via ArgoCD
# See: kubernetes/environments/prod/superset-app.yaml
# Note: Secrets are managed by Infisical
# See: kubernetes/infrastructure/infisical/superset-infisical-secret.yaml

